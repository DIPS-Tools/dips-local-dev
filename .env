# Update these values for your local environment before running Docker Compose.

# General
HOST_ADDR=10.22.13.250 (<CHANGE_ME_WITH_YOUR_MACHINE_IP>)


# Emulator settings
USE_EMULATOR=true
VITE_USE_EMULATOR=true
PORT=8019 (<CHANGE_ME_WITH_A_APPROPRIATE_IP>)

# Keycloak settings (Update these values ACCORDING TO YOUR KEYCLOAK SETTINGS)
REALMS_NAME=dips_services
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=123456

# Shared application credentials
#(keep it at the moment, we will remove it in the future)
MASTER_PASSWORD=5hnd..jk4ne!kwjs?wnsmmf

# MongoDB settings
MONGO_USER=root
MONGO_PASSWORD=<CHANGE_ME_MONGO_PASSWORD>
MONGO_HOST=mongo
MONGO_PORT=27017
MONGO_DB=dips_services
# user's info is also saved in dips_services
USER_MONGO_DB=dips_services

# Negotiation dashboard test accounts (this is used in the UPCAST project, will remove it in the future)
CONSUMER_DASHBOARD_USERNAME="Semih.Yumusak@soton.ac.uk"
CONSUMER_DASHBOARD_PASSWORD="wcXaWTm#ex.`Q/-q7UHZEL"
PROVIDER_DASHBOARD_USERNAME="giannis.papastamou@maggioli.gr"
PROVIDER_DASHBOARD_PASSWORD="12345678"

#Optional AI settings (it is unnecessary)
OPENAI_API_KEY=
OPENAI_AGENT_MODEL=gpt-4o-mini
